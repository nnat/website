<!-- Static navbar -->
<nav class="navbar header-v1 header-transparent navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header scroll-to">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" style="background-color: white">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to image_tag("home/new_logo.png", width: '50', style: "margin-top:5px"), root_path, alt: 'Risebox' %>
            <!-- <a class="navbar-brand" href="index.html">Risebox</a> -->
        </div>
        <%# link_to image_tag("home/new_logo.png", width: '50'), root_path %>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav scroll-to navbar-nav"> 
                <li class="active"><%= link_to raw(t('.home')), root_path, id: 'home-nav-menu' %></li>
                <li class="<%= 'active' if (@section == :specs)%>" ><%= link_to raw(t('.specs')), product_path, id: 'specs-nav-menu' %></li>
                <li class="<%= 'active' if (@section == :services)%>" ><%= link_to raw(t('.services')), services_path, id: 'services-nav-menu' %></li>
                <li class="<%= 'active' if (@section == :office)%>" ><%= link_to raw(t('.office')), office_path, id: 'office-nav-menu' %></li>
                <li class="<%= 'active' if (@section == :mission)%>" ><%= link_to raw(t('.mission')), mission_path, id: 'mission-nav-menu' %></li>
                <li class="<%= 'active' if (@section == :faq)%>" ><%= link_to raw(t('.faq')), faq_path, id: 'faq-nav-menu' %></li>
                <!--
                <li class="dropdown">
                    <a class="dropdown-toggle" data-target="#" data-toggle="dropdown">
                      <%= I18n.locale == :fr ? 'FR' : 'EN' %> <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                        <li role="presentation"><a role="menuitem" tabindex="-1" href="<%= url_for(locale: (I18n.locale == :fr ? :en : :fr)) %>"><%= I18n.locale == :fr ? 'EN' : 'FR' %></a></li>
                    </ul>
                </li> -->
            </ul>
            <ul class="nav navbar-nav navbar-right"> 
                <% unless @section == :program %>
                    <li class="button-nav"><%= link_to t('.program'), versions_path, id: 'program-nav-menu', class: "btn btn-warning btn-lg" %></li>
                <% end %>
            </ul>
        </div><!--/.nav-collapse -->
    </div><!--/.container-fluid -->
</nav><!--navigation end here-->